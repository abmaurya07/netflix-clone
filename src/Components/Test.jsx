import React from 'react'

const Test = ({val}) => {
  return (
    <div style={{display:'flex', justifyContent:'center', alignItems:'center'}}>
        <div>
    <h1 >{val}</h1>
    </div>
    </div>
  )
}

export default Test